# zo.computer Proxy 配置
# 多个 token 负载均衡，每个 token 对应不同域名
ZO_TOKENS=[
  {"token":"token1","origin":"https://rustydaisy.zo.computer"},
  {"token":"token2","origin":"https://adsa.zo.computer"}
]

# 服务端口（默认 3000）
PORT=3000
